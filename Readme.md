1. Clone the Repository
```bash
git clone https://github.com/Ntombenhle2004/Alumni-directory-and-communication-portal.git

```

2. Checkout to the Assigned Branch
```bash
git checkout team-work
```

3. Install Dependencies
```bash
python -m venv .venv
source ./.venv/Scripts/activate
pip install -r requirements.txt
pip install request
cd backend
python run.py
```